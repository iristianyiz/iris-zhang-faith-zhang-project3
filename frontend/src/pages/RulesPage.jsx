export function RulesPage() {
  return (
    <section>
      <h1>Rules</h1>
      <p>
        Each row, column, and sub-grid must contain the digits 1 through N
        exactly once (N is the board size).
      </p>
      <h2>Credits</h2>
      <p>Made by — add links (email, GitHub, LinkedIn) here.</p>
    </section>
  );
}
